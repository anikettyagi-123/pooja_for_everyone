

import 'package:flutter/material.dart';

class samagrhi extends StatelessWidget {
  const samagrhi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:   Column(
    children: [
    Center(child: Text('Samagrhi'))
    ],
    ),
    );
  }
}
